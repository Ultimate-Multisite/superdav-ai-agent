node_modules/
# Composer dev dependencies are excluded — production vendor/ is committed
# Run `composer install --no-dev` to regenerate
.agents/loop-state/
.phpunit.result.cache
screenshots/
.aidevops.json
.beads
coverage.xml
coverage-html/
# Playwright
playwright-report/
test-results/
blob-report/
# Benchmark results (contain API-specific data, regenerated on demand)
tests/benchmark/results/
# Local AI tooling artifacts
.claude/
.agents/skills/
skills-lock.json
# Local wp-env port overrides
.wp-env.override.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Build artifacts
*.zip
# Dev-only composer packages (not needed in production)
vendor/bin/
vendor/dealerdirect/
vendor/doctrine/
vendor/myclabs/
vendor/nikic/
vendor/phar-io/
vendor/php-stubs/
vendor/phpcompatibility/
vendor/phpcsstandards/
vendor/phpstan/
vendor/phpunit/
vendor/sebastian/
vendor/squizlabs/
vendor/szepeviktor/
vendor/theseer/
vendor/wp-coding-standards/
vendor/yoast/
