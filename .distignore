# Dev files excluded from distribution
# Generated by t059 — keep in sync with bin/build.sh additional exclusions

# Version control
.git
.gitignore
.gitattributes

# This file
.distignore

# Node / build tooling
node_modules
src
package.json
package-lock.json
webpack.config.js

# Composer dev metadata (vendor/ itself is included; only the manifests are excluded)
composer.json
composer.lock

# PHP quality / test tooling
phpunit.xml
phpunit.xml.dist
phpcs.xml
phpstan.neon
phpstan.neon.dist

# JavaScript / CSS linting
.eslintrc
.eslintrc.json
.eslintrc.js
.stylelintrc
.stylelintrc.json
.stylelintrc.js
.prettierrc
.prettierrc.json
.prettierrc.js

# Editor / IDE config
.editorconfig
.cursorrules
.clinerules
.windsurfrules

# WordPress local dev environment
.wp-env.json

# CI / GitHub Actions
.github

# Tests
tests

# Dev scripts
bin

# Playground / local dev blueprints
playground

# Database seeds and migration helpers (dev-only)
seeds
migrations

# JSON schemas (dev reference only)
schemas

# AI agent / automation tooling
.agents
.claude
.husky
AGENTS.md

# Documentation (not needed in plugin zip)
README.md
ROADMAP.md
CONTRIBUTING.md
CODE_OF_CONDUCT.md
SECURITY.md
CHANGELOG.md
PLANS-AI-AGENT-MASTERPLAN.md

# Task management
TODO.md
todo
.task-counter

# Dev screenshots (root-level; assets/screenshots/ is kept)
screenshots

# PHPUnit cache
.phpunit.result.cache
.phpunit.cache

# Vendor dev-only packages (safety net — prefer composer install --no-dev)
# These are require-dev packages that should not ship in a production zip.
vendor/dealerdirect
vendor/doctrine
vendor/myclabs
vendor/nikic
vendor/phar-io
vendor/phpcompatibility
vendor/phpcsstandards
vendor/phpstan
vendor/php-stubs
vendor/phpunit
vendor/sebastian
vendor/squizlabs
vendor/szepeviktor
vendor/theseer
vendor/wp-coding-standards
vendor/yoast

# Build artefacts that should not be re-packaged
*.zip
*.map

# Misc
*.log
.DS_Store
Thumbs.db
